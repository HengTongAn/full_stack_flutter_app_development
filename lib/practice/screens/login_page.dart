import 'package:flutter/material.dart';

class LoginAnotherPage extends StatelessWidget {
  const LoginAnotherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Center(
        child: Text("Login Page"),
      ),
      ),
    );
  }
}
